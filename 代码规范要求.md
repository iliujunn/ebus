# 新能源公交车队智能充电调度项目代码规范要求

## 1. 总体原则

本项目代码应满足以下目标：

1. **可读性优先**：代码结构清晰，变量命名准确，便于组员理解和维护。
2. **可复现实验**：同一组输入数据和随机种子下，应尽量得到一致结果。
3. **模块化设计**：数据生成、能耗预测、JSQ 基线、遗传算法、评估和可视化应分模块实现。
4. **高效计算**：对能并行的任务尽量使用现代多核 CPU 或 GPU 进行加速。
5. **结果可追踪**：每次实验的配置、指标、图表和输出文件应有明确记录。
6. **避免过度复杂**：优先实现稳定、可解释、可展示的版本，再逐步优化性能。

## 2. 推荐项目结构

建议采用如下目录结构：

```text
project/
  data/
    raw/
    processed/
    simulated/

  src/
    config.py
    simulate_data.py
    energy_model.py
    baseline_jsq.py
    ga_optimizer.py
    evaluation.py
    visualization.py
    utils.py
    main.py

  outputs/
    figures/
    schedules/
    metrics/
    models/

  tests/
    test_energy_model.py
    test_baseline_jsq.py
    test_ga_optimizer.py
    test_evaluation.py

  docs/
    代码规范要求.md
    项目说明.md

  environment.yml
  Makefile
  README.md
```

说明：

| 目录/文件 | 作用 |
|---|---|
| data/raw | 原始 GTFS 或其他原始数据 |
| data/processed | 清洗后的数据 |
| data/simulated | 模拟生成的数据 |
| src/config.py | 全局配置参数 |
| src/simulate_data.py | 生成模拟数据 |
| src/energy_model.py | 神经网络能耗预测模型 |
| src/baseline_jsq.py | JSQ 负载均衡基线 |
| src/ga_optimizer.py | 遗传算法调度优化 |
| src/evaluation.py | 指标计算 |
| src/visualization.py | 甘特图、SOC 曲线、对比图 |
| outputs | 所有运行结果 |
| tests | 单元测试 |
| environment.yml | Conda 环境定义 |
| Makefile | 统一运行命令 |

## 3. 命名规范

### 3.1 文件命名

Python 文件使用小写字母和下划线：

```text
energy_model.py
baseline_jsq.py
ga_optimizer.py
```

不建议使用：

```text
EnergyModel.py
GA-Optimizer.py
代码1.py
最终版.py
```

### 3.2 变量命名

变量名应表达业务含义：

```python
battery_capacity_kwh = 300
current_soc = 0.65
charging_power_kw = 120
predicted_energy_kwh = 18.5
```

不建议：

```python
a = 300
x = 0.65
tmp = 18.5
```

### 3.3 函数命名

函数使用动宾结构，表达清楚动作：

```python
generate_trip_data()
train_energy_model()
run_jsq_scheduler()
run_ga_optimizer()
calculate_total_cost()
plot_gantt_chart()
```

### 3.4 类命名

类名使用大驼峰命名：

```python
EnergyPredictor
JSQScheduler
GAOptimizer
ChargingStation
ElectricBus
```

## 4. 可读性要求

### 4.1 函数长度

单个函数尽量控制在 80 行以内。

如果函数过长，应拆分为多个小函数：

```python
def run_experiment(config):
    trips = load_trip_data(config)
    model = train_energy_model(config)
    jsq_result = run_jsq_scheduler(trips, config)
    ga_result = run_ga_optimizer(trips, config)
    evaluate_results(jsq_result, ga_result, config)
```

### 4.2 避免重复代码

重复出现 2 次以上的逻辑应考虑封装为函数。

例如，SOC 更新逻辑应统一封装：

```python
def update_soc_after_trip(current_soc, energy_used_kwh, battery_capacity_kwh):
    return current_soc - energy_used_kwh / battery_capacity_kwh
```

### 4.3 注释要求

注释应解释“为什么这样做”，而不是重复代码含义。

推荐：

```python
# 未完成班次会严重影响公交运营，因此惩罚权重设置得远高于电费成本。
unfinished_trip_penalty = 10000
```

不推荐：

```python
# 设置未完成班次惩罚
unfinished_trip_penalty = 10000
```

### 4.4 类型标注

核心函数建议添加类型标注：

```python
def calculate_charging_time(
    required_energy_kwh: float,
    charging_power_kw: float
) -> float:
    return required_energy_kwh / charging_power_kw * 60
```

### 4.5 文档字符串

重要函数需要写 docstring：

```python
def run_jsq_scheduler(trips, vehicles, stations, config):
    """
    Run the JSQ baseline scheduler.

    Vehicles select the charging queue with the minimum estimated completion time.
    Queue discipline inside each charging queue follows FCFS.
    """
```

## 5. 数据处理规范

### 5.1 数据字段统一

所有数据字段命名应统一使用英文小写和下划线：

```text
trip_id
route_id
bus_id
station_id
start_time
end_time
distance_km
energy_kwh
current_soc
charging_power_kw
```

### 5.2 单位必须写入字段名

涉及物理量时，字段名必须包含单位：

```text
distance_km
energy_kwh
charging_power_kw
duration_min
cost_yuan
carbon_kg
```

避免：

```text
distance
energy
power
duration
cost
```

### 5.3 原始数据与处理数据分离

原始数据不得被程序覆盖。

推荐：

```text
data/raw/gtfs_trips.csv
data/processed/trips_cleaned.csv
data/simulated/trips_simulated.csv
```

### 5.4 数据校验

读取数据后必须检查：

```text
是否存在缺失值
时间字段是否可解析
SOC 是否在 0-1 之间
电池容量是否大于 0
充电功率是否大于 0
班次结束时间是否晚于开始时间
```

## 6. 配置管理规范

所有关键参数应放在统一配置文件中，避免散落在代码里。

示例：

```python
BATTERY_CAPACITY_KWH = 300
MIN_SOC = 0.2
CHARGE_TRIGGER_SOC = 0.3
TARGET_SOC = 0.85

FAST_CHARGING_POWER_KW = 120
SLOW_CHARGING_POWER_KW = 40

TIME_STEP_MIN = 10
RANDOM_SEED = 42

UNFINISHED_TRIP_PENALTY = 10000
LOW_SOC_PENALTY = 1000
```

配置要求：

1. 不允许在多个文件中重复定义同一个参数。
2. 实验参数修改应优先修改配置文件。
3. 每次实验应保存一份实际使用的配置。

## 6.5 Conda 环境与 Makefile 统一运行命令

项目必须使用独立 Conda 环境，避免不同组员机器上的 Python 版本和依赖版本不一致。

推荐环境名称：

```text
ebus-dispatch
```

环境文件必须放在项目根目录：

```text
environment.yml
```

创建环境：

```bash
make env
```

更新环境：

```bash
make env-update
```

激活环境：

```bash
conda activate ebus-dispatch
```

所有常用实验命令必须通过 `Makefile` 统一管理，避免每个人手工输入不同命令。

推荐命令：

```bash
make setup       # 创建必要目录
make data        # 采集或处理真实 GTFS 数据
make simulate    # 生成模拟数据
make train       # 训练能耗预测模型
make jsq         # 运行 JSQ 基线调度
make ga          # 运行遗传算法优化调度
make evaluate    # 统计实验指标
make figures     # 生成图表
make run         # 运行完整实验流程
make test        # 运行测试
make format      # 格式化代码
make lint        # 静态检查
make clean       # 清理缓存文件
```

要求：

1. 组员运行项目时，优先使用 `make` 命令。
2. `Makefile` 中的命令应调用 Conda 环境内的 Python。
3. 新增核心模块后，应同步补充或更新对应的 Makefile 目标。
4. `make run` 应作为完整实验的一键入口。
5. `make test`、`make format`、`make lint` 应作为提交前的质量检查入口。
6. 不建议在汇报前临时手工运行一串分散命令，应尽量让结果由 `make run` 自动生成。

## 7. 可复现性要求

### 7.1 固定随机种子

涉及随机生成、神经网络训练、遗传算法初始化时，应统一设置随机种子。

```python
import random
import numpy as np
import torch

def set_random_seed(seed: int) -> None:
    random.seed(seed)
    np.random.seed(seed)
    torch.manual_seed(seed)
    if torch.cuda.is_available():
        torch.cuda.manual_seed_all(seed)
```

### 7.2 保存实验输出

每次实验应保存：

```text
配置参数
数据版本
随机种子
模型指标
调度指标
输出图表
运行时间
```

建议输出到：

```text
outputs/metrics/experiment_summary.json
outputs/figures/
outputs/schedules/
```

### 7.3 避免手工修改结果

图表、表格和指标应由代码自动生成，避免手工改数。

## 8. 并行化与性能优化要求

本项目包含大量可并行任务，例如能耗预测、遗传算法适应度评估、多车辆调度仿真和多路径能耗评估。应尽量利用现代多核 CPU 或 GPU 的并行能力。

### 8.1 优先级原则

性能优化顺序建议为：

```text
先保证结果正确
再使用向量化
再使用多进程/多线程
最后考虑 GPU 加速
```

不要一开始就为了并行化牺牲代码可读性。

### 8.2 使用 NumPy/Pandas 向量化

能用数组运算的地方，不要写大量 Python for 循环。

推荐：

```python
df["energy_kwh"] = (
    1.1 * df["distance_km"]
    + 0.4 * df["congestion_index"] * df["distance_km"]
    + 0.2 * df["passenger_load"] * df["distance_km"]
)
```

不推荐：

```python
for i in range(len(df)):
    df.loc[i, "energy_kwh"] = ...
```

### 8.3 遗传算法适应度并行计算

遗传算法中，不同个体的适应度计算通常互相独立，应优先并行化。

推荐使用：

```text
concurrent.futures.ProcessPoolExecutor
joblib.Parallel
multiprocessing
```

示例：

```python
from concurrent.futures import ProcessPoolExecutor

def evaluate_population(population, static_data, num_workers):
    with ProcessPoolExecutor(max_workers=num_workers) as executor:
        scores = list(
            executor.map(
                evaluate_individual,
                population,
                [static_data] * len(population)
            )
        )
    return scores
```

要求：

1. 适应度函数应尽量设计为纯函数。
2. 不同进程之间不要共享可变全局变量。
3. 并行计算结果顺序应与输入个体顺序一致。
4. 并行版本和串行版本应输出一致或近似一致的结果。

### 8.4 多路径能耗预测批量化

绿色路径规划中，多个候选路径的能耗预测应批量输入神经网络。

推荐：

```python
features_tensor = torch.tensor(path_features, dtype=torch.float32)
predicted_energy = model(features_tensor)
```

不推荐每条路径单独调用一次模型。

### 8.5 GPU 使用要求

神经网络训练和批量推理可以使用 GPU。

代码应自动判断是否可用：

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
model = model.to(device)
```

如果使用 Mac，可兼容 MPS：

```python
if torch.cuda.is_available():
    device = torch.device("cuda")
elif torch.backends.mps.is_available():
    device = torch.device("mps")
else:
    device = torch.device("cpu")
```

要求：

1. 模型和数据必须放在同一个 device 上。
2. 不要在训练循环中频繁进行 CPU/GPU 数据来回拷贝。
3. 推理阶段使用 `torch.no_grad()`。
4. 小规模数据不强制使用 GPU，因为数据传输开销可能大于加速收益。

推理示例：

```python
model.eval()
with torch.no_grad():
    predictions = model(features.to(device))
```

### 8.6 多核 CPU 使用要求

适合使用多核 CPU 的任务：

```text
遗传算法个体适应度计算
多组实验重复运行
不同随机种子实验
不同参数组合实验
批量生成模拟数据
批量绘图
```

不建议并行化的任务：

```text
很小规模的数据处理
有强顺序依赖的车辆时间线仿真
频繁读写同一个文件的任务
```

### 8.7 并行安全要求

并行代码必须避免：

```text
多个进程同时写同一个文件
多个进程修改同一个全局变量
多个进程使用不受控随机数
日志输出互相覆盖
```

建议：

```text
每个进程只返回计算结果
主进程统一汇总结果
主进程统一保存文件
每个进程使用独立随机种子
```

### 8.8 性能记录要求

关键模块应记录运行时间：

```python
import time

start = time.perf_counter()
result = run_ga_optimizer(config)
elapsed = time.perf_counter() - start
print(f"GA optimizer finished in {elapsed:.2f} seconds")
```

至少记录：

```text
数据生成时间
模型训练时间
JSQ 调度时间
GA 优化时间
图表生成时间
```

## 9. 算法实现规范

### 9.1 JSQ 基线实现要求

JSQ 基线应满足：

1. 车辆触发充电请求后，选择预计完成时间最短的充电位置。
2. 每个队列内部默认 FCFS。
3. 不允许插队。
4. 不考虑未来全局优化，只使用当前队列状态。
5. 输出等待时间、充电开始时间、充电结束时间和 SOC 变化。

充电位置选择公式：

```text
j* = argmin_j (T_travel,j + T_wait,j + T_charge,j)
```

### 9.2 遗传算法实现要求

遗传算法应包含：

```text
个体编码
初始化
适应度评估
选择
交叉
变异
约束修复
最优个体保存
迭代日志记录
```

每一代至少记录：

```text
generation
best_cost
mean_cost
unfinished_trips
total_charging_cost
```

### 9.3 约束处理要求

对于不可行方案，必须明确处理方式：

1. 可以修复的，优先修复。
2. 无法修复的，给予高额惩罚。
3. 不允许静默忽略约束冲突。

常见冲突：

```text
车辆同时充电和运行
车辆 SOC 低于安全阈值
车辆 SOC 超过 100%
充电桩同时服务车辆数超过容量
车辆无法赶上下一个班次
```

### 9.4 能耗模型实现要求

能耗预测模型应包含：

```text
训练集/测试集划分
特征归一化
模型训练
模型评估
模型保存
预测接口
```

训练时必须输出：

```text
训练损失
测试 MAE
测试 RMSE
测试 R²
```

## 10. 日志规范

项目应使用 `logging`，不要大量使用 `print`。

推荐：

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(message)s"
)

logger = logging.getLogger(__name__)
logger.info("Start training energy model")
```

日志内容应包括：

```text
实验开始和结束
读取数据路径
关键配置参数
训练指标
调度指标
异常信息
输出文件路径
```

## 11. 异常处理规范

代码中应对常见错误进行明确提示。

例如：

```python
if battery_capacity_kwh <= 0:
    raise ValueError("battery_capacity_kwh must be positive")

if not 0 <= current_soc <= 1:
    raise ValueError("current_soc must be between 0 and 1")
```

不建议：

```python
try:
    ...
except:
    pass
```

异常信息应说明：

```text
哪里出错
为什么出错
如何检查
```

## 12. 测试要求

至少对核心函数写简单测试。

### 12.1 单元测试范围

建议测试：

```text
SOC 更新是否正确
充电时间计算是否正确
JSQ 是否选择等待时间最短队列
成本函数是否正确累加
遗传算法是否能输出合法方案
能耗模型输入输出维度是否正确
```

### 12.2 测试示例

```python
def test_calculate_charging_time():
    required_energy_kwh = 60
    charging_power_kw = 120
    duration_min = calculate_charging_time(required_energy_kwh, charging_power_kw)
    assert duration_min == 30
```

### 12.3 回归测试

每次修改算法后，应至少运行一个小规模实验，确保：

```text
程序能完整跑通
输出文件正常生成
指标没有明显异常
没有出现负 SOC
没有出现充电桩超容量
```

## 13. 可视化规范

所有图表必须满足：

1. 标题清楚。
2. 坐标轴有单位。
3. 图例清晰。
4. 文件名能表达内容。
5. JSQ 和 GA 对比图使用一致的颜色和尺度。

推荐文件命名：

```text
gantt_jsq.png
gantt_ga.png
soc_curve_ga.png
cost_comparison.png
energy_prediction_comparison.png
path_energy_comparison.png
```

甘特图颜色建议：

| 状态 | 颜色 |
|---|---|
| 执行班次 | 蓝色 |
| 慢充 | 绿色 |
| 快充 | 橙色 |
| 等待/空闲 | 灰色 |
| 电量不足/未完成 | 红色 |

## 14. 输出结果规范

调度结果应保存为结构化文件，例如 CSV 或 JSON。

建议字段：

```text
bus_id
event_type
start_time
end_time
station_id
charger_type
soc_before
soc_after
energy_kwh
cost_yuan
trip_id
```

指标结果建议保存：

```text
total_cost
charging_cost
time_cost
penalty_cost
unfinished_trips
average_waiting_time_min
fast_charging_count
slow_charging_count
average_soc
total_energy_kwh
total_carbon_kg
runtime_seconds
```

## 15. 版本管理规范

建议使用 Git 管理代码。

提交信息应清楚表达本次修改：

```text
Add JSQ baseline scheduler
Implement GA fitness evaluation
Add energy prediction model
Fix SOC update bug
Generate Gantt chart visualization
```

不建议：

```text
update
fix
final
final2
改了一点
```

## 16. 代码风格工具

如果时间允许，可以使用以下工具统一风格：

```text
black：自动格式化 Python 代码
isort：整理 import 顺序
flake8 或 ruff：检查代码问题
pytest：运行测试
```

推荐命令：

```bash
black src tests
isort src tests
pytest
```

## 17. 禁止事项

项目中应避免以下写法：

1. 大量无意义变量名，例如 `a`、`b`、`x1`、`tmp2`。
2. 所有逻辑都写在一个 `main.py` 文件中。
3. 参数硬编码散落在多个文件。
4. 手工修改实验结果。
5. 忽略 SOC 越界或充电桩超容量问题。
6. 多进程同时写同一个输出文件。
7. 使用裸 `except` 后不处理异常。
8. 训练模型但不保存随机种子和配置。
9. 输出图表没有单位、标题和图例。
10. 算法结果只展示最优值，不记录过程指标。

## 18. 最终检查清单

提交代码前，应检查：

```text
代码能从 main.py 一键运行
随机种子已设置
关键参数位于 config.py
JSQ 基线能正常输出结果
GA 优化能正常输出结果
神经网络能耗模型有评价指标
所有图表都能自动生成
输出结果保存在 outputs 目录
没有明显 SOC 越界
没有充电桩容量冲突
实验指标和图表能支撑汇报结论
```

## 19. 简短总结

本项目代码应做到：

```text
结构清楚、命名准确、结果可复现、算法可解释、计算可并行、图表可汇报。
```

其中，并行化重点放在：

```text
遗传算法适应度评估
多路径能耗批量预测
多组实验重复运行
神经网络 GPU 训练和推理
```

JSQ 和 GA 的核心调度逻辑应优先保证正确性，再逐步进行性能优化。
