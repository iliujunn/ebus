# 案例7：新能源公交车队的智能充电调度与绿色路径规划任务规划

作者：刘俊、戴维昌

## 1. 项目定位

本项目面向新能源公交车队运营场景，研究在多条公交线路、多辆纯电动公交车、多个充电站以及分时电价条件下，如何进行智能充电调度与绿色路径规划。

项目核心目标是：

1. 使用简单神经网络预测每趟公交行程的能耗。
2. 使用遗传算法优化车辆充电时间、充电位置、充电方式和充电量。
3. 使用 JSQ 负载均衡策略作为传统基线方案。
4. 对比优化前后在充电成本、等待时间、未完成班次、车辆 SOC 和能耗方面的差异。

整体技术路线可以概括为：

```text
公交班次数据
    ↓
模拟坡度、拥堵、载客率、温度等能耗影响因素
    ↓
神经网络预测单趟行程能耗
    ↓
JSQ 基线调度 / 遗传算法优化调度
    ↓
输出甘特图、SOC 曲线、成本对比、能耗对比和路径能耗分析
```

## 2. 问题边界与业务场景

为了保证任务可完成，项目不建议直接做全城市级公交调度，而是构建一个中小规模模拟场景。

推荐场景设定如下：

| 对象 | 建议设置 |
|---|---|
| 公交线路 | 3 条 |
| 公交车辆 | 10-20 辆 |
| 充电站 | 2-3 个 |
| 充电桩类型 | 快充、慢充 |
| 调度周期 | 一天，5:00-23:00 |
| 时间粒度 | 10 分钟或 15 分钟 |
| 电池容量 | 300 kWh |
| SOC 安全下限 | 20% |
| 充电触发阈值 | 30% |
| 目标 SOC | 80%-90% |

充电站可以设置为：

| 充电站 | 位置 | 快充桩数量 | 慢充桩数量 |
|---|---|---:|---:|
| Station A | 公交总站 | 4 | 6 |
| Station B | 中途枢纽站 | 2 | 2 |
| Station C | 末端站 | 1 | 3 |

这样可以自然引入多充电位置、多充电队列和负载均衡问题。

## 3. 数据准备

数据可以采用“开源数据结构 + 模拟业务数据”的方式完成。

如果能找到 GTFS 数据，可以使用 GTFS 中的线路、站点和班次信息。如果时间有限，也可以直接模拟班次数据，重点是保证字段完整、逻辑合理。

### 3.1 公交班次数据

每趟公交行程建议包含以下字段：

| 字段 | 含义 |
|---|---|
| trip_id | 班次编号 |
| route_id | 线路编号 |
| start_time | 发车时间 |
| end_time | 到达时间 |
| origin | 起点 |
| destination | 终点 |
| distance_km | 行驶距离 |
| candidate_path | 候选路径编号 |

示例：

```text
Trip T001：Route 1，06:30-07:10，12.5 km
Trip T002：Route 1，07:20-08:05，13.1 km
Trip T003：Route 2，07:00-07:50，15.8 km
```

### 3.2 车辆数据

车辆数据建议包含：

| 字段 | 含义 |
|---|---|
| bus_id | 车辆编号 |
| battery_capacity | 电池容量 |
| initial_soc | 初始电量比例 |
| max_soc | 最大允许 SOC |
| min_soc | 最小安全 SOC |
| assigned_route | 车辆主要服务线路 |

### 3.3 充电站数据

充电站数据建议包含：

| 字段 | 含义 |
|---|---|
| station_id | 充电站编号 |
| station_name | 充电站名称 |
| location | 位置 |
| fast_chargers | 快充桩数量 |
| slow_chargers | 慢充桩数量 |
| fast_power | 快充功率 |
| slow_power | 慢充功率 |

示例：

```text
快充功率：120 kW
慢充功率：40 kW
```

### 3.4 分时电价数据

分时电价可以设定为：

| 时段 | 类型 | 电价 |
|---|---|---:|
| 00:00-07:00 | 谷时段 | 0.45 元/kWh |
| 07:00-10:00 | 峰时段 | 1.20 元/kWh |
| 10:00-17:00 | 平时段 | 0.80 元/kWh |
| 17:00-21:00 | 峰时段 | 1.20 元/kWh |
| 21:00-24:00 | 平/谷时段 | 0.60 元/kWh |

### 3.5 能耗影响因素数据

每趟行程需要构造以下特征：

| 特征 | 含义 |
|---|---|
| distance_km | 行驶里程 |
| avg_slope | 平均坡度 |
| congestion_index | 拥堵指数 |
| avg_speed | 平均速度 |
| passenger_load | 载客率 |
| temperature | 温度 |
| peak_hour | 是否高峰时段 |

可以用模拟耗电模型生成训练标签：

```text
energy = 1.1 * distance
       + 0.25 * positive_slope * distance
       + 0.4 * congestion_index * distance
       + 0.2 * passenger_load * distance
       + temperature_penalty
       + noise
```

其中：

```text
energy：该趟行程真实耗电量，单位 kWh
distance：行驶距离，单位 km
positive_slope：正坡度影响
congestion_index：拥堵指数，范围 0-1
passenger_load：载客率，范围 0-1
noise：随机扰动项
```

## 4. 能耗预测模型

能耗预测模型负责根据行程特征预测该趟公交的耗电量。

### 4.1 输入与输出

输入特征：

```text
[里程, 坡度, 拥堵指数, 平均速度, 载客率, 温度, 是否高峰]
```

输出：

```text
预测能耗 kWh
```

### 4.2 模型结构

建议使用简单的多层感知机 MLP：

```text
Input
  ↓
Dense(64) + ReLU
  ↓
Dense(32) + ReLU
  ↓
Dense(1)
  ↓
Predicted Energy
```

损失函数：

```text
MSE
```

评价指标：

```text
MAE
RMSE
R²
```

### 4.3 对比模型

为了证明神经网络有价值，需要设置一个简单对比模型：

```text
固定单位里程耗电模型：
energy = fixed_consumption_per_km * distance
```

例如：

```text
每公里固定耗电 1.2 kWh
```

结果展示可以采用：

| 模型 | MAE | RMSE |
|---|---:|---:|
| 固定单位里程耗电模型 | 5.8 kWh | 7.2 kWh |
| 神经网络预测模型 | 2.4 kWh | 3.1 kWh |

结论表达：

```text
神经网络模型考虑了坡度、拥堵、载客率和温度等因素，因此能更准确地预测公交行程能耗。
```

## 5. 优化前基线方案：JSQ 负载均衡策略

优化前方案不采用全局智能优化，而是采用传统的局部负载均衡策略。

基线名称建议写为：

```text
基于 JSQ 的局部负载均衡充电调度策略
```

JSQ 是 Join the Shortest Queue，即车辆到达后加入预计等待时间最短的队列。

### 5.1 FCFS 与 JSQ 的关系

在本项目中，FCFS 可以视为默认队列纪律：

```text
同一个充电队列内部，默认先到先充，不允许插队。
```

JSQ 才是基线方案的核心决策规则：

```text
新到达的车辆应该进入哪个充电站或哪个充电桩队列。
```

### 5.2 JSQ 决策公式

如果存在多个充电站，则车辆选择：

```text
j* = argmin_j (T_travel,j + T_wait,j + T_charge,j)
```

其中：

```text
T_travel,j：车辆到充电站 j 的行驶时间
T_wait,j：充电站 j 当前预计排队等待时间
T_charge,j：车辆在充电站 j 的预计充电时间
```

如果车辆已经在同一个公交场站内，则可以简化为：

```text
j* = argmin_j (T_wait,j + T_charge,j)
```

### 5.3 JSQ 调度流程

```text
1. 车辆按固定班表运行。
2. 每完成一趟行程后，根据预测能耗更新 SOC。
3. 如果 SOC < 30%，触发充电请求。
4. 系统计算每个充电站或充电桩队列的预计等待时间。
5. 车辆选择预计完成时间最短的位置充电。
6. 队列内部默认采用 FCFS。
7. 车辆充到目标 SOC 或下一班发车前离开。
8. 如果 SOC 不足以完成下一班，则记录为未完成班次。
```

### 5.4 JSQ 的优缺点

优点：

1. 实现简单。
2. 能平衡当前充电队列压力。
3. 符合实际充电站排队逻辑。

缺点：

1. 只关注当前等待时间。
2. 不考虑未来班次紧急程度。
3. 不考虑分时电价。
4. 不考虑未来能耗变化。
5. 可能导致当前最优但全局不优。

因此，JSQ 很适合作为优化前的 baseline。

## 6. 优化算法：遗传算法 GA

主优化算法建议选择遗传算法，而不是 DQN 或 MILP。

原因：

1. 遗传算法适合离散组合优化问题。
2. 建模难度低于强化学习。
3. 汇报时更容易解释。
4. 代码实现相对稳定。
5. 能自然处理充电站选择、充电方式选择和充电时间安排。

### 6.1 优化目标

优化目标是最小化总成本：

```text
Cost =
Σ(充电量 × 分时电价)
+ α × Σ(等待时间 + 充电占用时间)
+ β × 未完成班次数
+ γ × SOC 低于安全阈值次数
```

其中：

```text
α：时间成本权重
β：未完成班次惩罚权重
γ：低电量风险惩罚权重
```

建议设置：

```text
β 远大于其他权重，例如 10000
```

因为公交班次未完成是严重运营问题。

### 6.2 个体编码

遗传算法中的一个个体代表一套完整充电调度方案。

可以将每个充电决策编码为：

```text
[bus_id, station_id, charger_type, start_time, charge_duration]
```

示例：

```text
Bus 01：Station A，慢充，10:30，40 min
Bus 02：Station B，快充，12:00，30 min
Bus 03：Station C，慢充，14:20，60 min
```

完整个体可以理解为：

```text
[
  Bus 01 的若干充电安排,
  Bus 02 的若干充电安排,
  ...
  Bus N 的若干充电安排
]
```

### 6.3 约束条件

需要满足以下约束：

```text
SOC 不能低于安全下限
SOC 不能超过电池容量
车辆不能同时执行班次和充电
车辆不能同时出现在两个充电站
充电桩同一时间不能超过容量
车辆必须在下一班发车前完成必要充电
快充和慢充功率不同
不同时间段电价不同
```

### 6.4 遗传算法流程

```text
1. 初始化一批随机可行调度方案。
2. 对每个方案进行调度仿真，计算总成本。
3. 根据成本计算适应度。
4. 选择成本较低的优秀个体。
5. 交叉：交换两个方案中的部分车辆充电安排。
6. 变异：随机修改充电站、充电类型、开始时间或充电时长。
7. 修复不可行方案。
8. 重复迭代若干代。
9. 输出总成本最低的调度方案。
```

### 6.5 GA 相比 JSQ 的优势

JSQ 是局部策略，只看当前哪条队列更短。

GA 是全局策略，可以同时考虑：

```text
未来班次
未来电价
车辆剩余电量
充电桩占用情况
快充与慢充选择
预测能耗
未完成班次风险
```

所以 GA 的目标不是让当前排队最短，而是让一整天的运营成本最低、未完成班次最少。

## 7. 绿色路径规划

绿色路径规划不必做成复杂地图导航，可以作为能耗预测模型的应用。

每趟公交行程设置 2-3 条候选路径：

| 路径 | 距离 | 坡度 | 拥堵 | 特点 |
|---|---:|---:|---:|---|
| Path A | 短 | 高 | 中 | 距离短但上坡多 |
| Path B | 中 | 低 | 低 | 路况稳定、省电 |
| Path C | 长 | 低 | 高 | 距离较长但坡度较低 |

对每条候选路径计算预测能耗：

```text
predicted_energy = NN(path_features)
```

选择最低能耗路径：

```text
path* = argmin_path predicted_energy(path)
```

碳排放可以用间接排放表示：

```text
carbon = predicted_energy × 电网碳排放因子
```

例如：

```text
电网碳排放因子 = 0.55 kgCO2/kWh
```

最终可以对比：

```text
最短距离路径 vs 最低能耗路径
```

说明并不是距离最短就一定最省电，上坡、拥堵和频繁启停都可能增加能耗。

## 8. 实验设计

项目至少做三组实验对比。

### 8.1 能耗预测对比

对比对象：

```text
固定单位里程耗电模型
神经网络能耗预测模型
```

指标：

```text
MAE
RMSE
R²
```

展示方式：

```text
真实能耗与预测能耗折线图
预测误差柱状图
模型评价指标表格
```

### 8.2 调度策略对比

对比对象：

```text
JSQ 负载均衡基线
遗传算法优化调度
```

指标：

| 指标 | 含义 |
|---|---|
| 总电费成本 | 所有充电产生的电费 |
| 总时间成本 | 等待时间和充电占用时间折算成本 |
| 未完成班次数 | 因电量不足无法完成的班次 |
| 快充次数 | 使用快充的次数 |
| 慢充次数 | 使用慢充的次数 |
| 平均等待时间 | 车辆进入充电队列后的平均等待 |
| 平均 SOC 安全余量 | 到站后剩余电量的安全程度 |
| 总成本 | 综合目标函数结果 |

展示表格示例：

| 指标 | JSQ 基线 | GA 优化 |
|---|---:|---:|
| 总电费成本 | 1250 元 | 980 元 |
| 总等待时间 | 420 min | 260 min |
| 快充次数 | 18 | 11 |
| 未完成班次 | 4 | 0 |
| 平均 SOC | 26% | 35% |
| 总成本 | 5300 | 3100 |

### 8.3 路径选择对比

对比对象：

```text
最短距离路径
最低能耗路径
```

指标：

```text
总行驶距离
总预测能耗
间接碳排放
平均行程时间
```

展示表格示例：

| 路径策略 | 总距离 | 总能耗 | 碳排放 |
|---|---:|---:|---:|
| 最短距离路径 | 820 km | 1040 kWh | 572 kgCO2 |
| 最低能耗路径 | 860 km | 960 kWh | 528 kgCO2 |

## 9. 可视化成果

最终成果建议至少包含以下图表。

### 9.1 调度方案甘特图

横轴：

```text
时间
```

纵轴：

```text
车辆编号
```

颜色含义：

| 颜色 | 含义 |
|---|---|
| 蓝色 | 执行公交班次 |
| 绿色 | 慢充 |
| 橙色 | 快充 |
| 灰色 | 等待或空闲 |
| 红色 | 电量不足或未完成班次 |

建议分别输出：

```text
gantt_jsq.png
gantt_ga.png
```

### 9.2 SOC 变化曲线

选择 3-5 辆代表车辆，展示一天内 SOC 的变化。

图中需要标注：

```text
行驶导致 SOC 下降
充电导致 SOC 上升
SOC 安全阈值
```

### 9.3 成本对比图

比较 JSQ 和 GA 的：

```text
电费成本
时间成本
惩罚成本
总成本
```

### 9.4 能耗预测图

展示：

```text
真实能耗曲线
神经网络预测能耗曲线
固定里程模型预测曲线
```

### 9.5 路径能耗对比图

展示每条候选路径的：

```text
距离
坡度
拥堵指数
预测能耗
碳排放
```

## 10. 代码模块规划

建议项目代码结构如下：

```text
project/
  data/
    trips.csv
    vehicles.csv
    stations.csv
    prices.csv
    energy_samples.csv

  src/
    simulate_data.py
    energy_model.py
    baseline_jsq.py
    ga_optimizer.py
    evaluation.py
    visualization.py
    main.py

  outputs/
    gantt_jsq.png
    gantt_ga.png
    soc_curve.png
    cost_comparison.png
    energy_prediction.png
    path_energy_comparison.png

  report/
    汇报PPT.pptx
```

各模块职责：

| 模块 | 作用 |
|---|---|
| simulate_data.py | 生成模拟线路、班次、车辆、充电站和能耗样本 |
| energy_model.py | 训练神经网络能耗预测模型 |
| baseline_jsq.py | 实现 JSQ 负载均衡基线调度 |
| ga_optimizer.py | 实现遗传算法优化调度 |
| evaluation.py | 统计成本、等待时间、未完成班次等指标 |
| visualization.py | 生成甘特图、SOC 曲线、成本对比图 |
| main.py | 串联整个实验流程 |

主程序流程：

```text
1. 读取或生成公交班次数据。
2. 生成能耗训练样本。
3. 训练神经网络能耗预测模型。
4. 用模型预测每趟行程耗电。
5. 运行 JSQ 基线调度。
6. 运行 GA 优化调度。
7. 统计两种方案的指标。
8. 输出甘特图、SOC 曲线和对比图。
```

## 11. 汇报 PPT 结构

PPT 可以按以下结构组织。

### 第 1 页：标题页

```text
案例7：新能源公交车队的智能充电调度与绿色路径规划
刘俊、戴维昌
```

### 第 2 页：研究背景

说明新能源公交调度面临的问题：

```text
电池容量有限
充电时间较长
充电桩资源有限
分时电价导致充电成本变化
坡度、拥堵和载客率导致能耗不确定
```

### 第 3 页：问题定义

说明输入、输出和优化目标：

```text
输入：班次、车辆、充电站、电价、路径和能耗特征
输出：充电调度方案和绿色路径选择方案
目标：降低成本、减少未完成班次、降低能耗和碳排放
```

### 第 4 页：系统整体框架

展示流程图：

```text
数据层 → 能耗预测层 → 调度优化层 → 结果评估层 → 可视化展示层
```

### 第 5 页：数据构建

介绍模拟数据和主要字段。

### 第 6 页：神经网络能耗预测

介绍输入特征、模型结构、评价指标。

### 第 7 页：JSQ 基线策略

说明：

```text
FCFS 是默认队列纪律
JSQ 负责新到车辆选择哪个充电站或队列
JSQ 是局部负载均衡策略
```

### 第 8 页：遗传算法优化调度

说明：

```text
个体编码
目标函数
约束条件
选择、交叉、变异、修复流程
```

### 第 9 页：绿色路径规划

说明：

```text
每趟行程存在多条候选路径
神经网络预测不同路径能耗
选择最低能耗路径
```

### 第 10 页：实验结果

展示：

```text
能耗预测对比
JSQ 与 GA 成本对比
未完成班次对比
SOC 曲线
```

### 第 11 页：甘特图展示

展示：

```text
JSQ 调度甘特图
GA 优化调度甘特图
```

### 第 12 页：结论

总结：

```text
神经网络提升了能耗预测精度。
GA 相比 JSQ 能降低总成本和未完成班次。
绿色路径选择能减少总能耗和间接碳排放。
```

## 12. 推荐完成顺序

建议按以下顺序推进：

1. 明确模拟场景参数，包括线路数、车辆数、充电站数、时间粒度和电价。
2. 编写 simulate_data.py，生成班次、车辆、充电站和能耗样本。
3. 先实现固定耗电模型，让整个调度流程能跑通。
4. 实现 JSQ 基线调度，并输出基础指标。
5. 训练神经网络能耗预测模型。
6. 将神经网络预测能耗接入调度流程。
7. 实现遗传算法优化调度。
8. 统计 JSQ 和 GA 的对比指标。
9. 增加绿色路径选择模块。
10. 输出甘特图、SOC 曲线和各种对比图。
11. 整理实验结果。
12. 制作 PPT 和最终汇报。

## 13. 项目最终交付物

最终应该交付：

1. 算法代码。
2. 模拟数据或 GTFS 处理后的数据。
3. 能耗预测模型结果。
4. JSQ 基线调度结果。
5. 遗传算法优化调度结果。
6. 调度方案甘特图。
7. SOC 变化曲线。
8. 成本对比图。
9. 能耗预测对比图。
10. 路径能耗与碳排放对比图。
11. 汇报 PPT。
12. 简短项目报告或说明文档。

## 14. 最终一句话总结

本项目可以概括为：

```text
以神经网络预测公交行程动态能耗，以 JSQ 负载均衡作为传统基线，以遗传算法进行全局充电调度优化，最终实现新能源公交车队低成本、高可靠、低能耗的智能运营方案。
```

